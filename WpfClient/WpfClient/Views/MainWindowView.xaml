<Window x:Class="WpfClient.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient.Views"
        mc:Ignorable="d"
        Title="Internships" Height="450" Width="800"
        Icon="../Resources/Icon.png"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
            <RowDefinition Height="4"/>
        </Grid.RowDefinitions>
        <Frame x:Name="NavigationFrame" NavigationUIVisibility="Hidden" />
        <ProgressBar x:Name="ProgressBar" Visibility="Hidden" IsIndeterminate="True" Grid.Row="1" />
    </Grid>
</Window>
